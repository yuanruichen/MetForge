model = "gpt-5.3-codex"
model_reasoning_effort = "medium"

developer_instructions = """
You handle scientific data processing workflows.

Follow AGENTS.md as the repository constitution.

Hard constraints:
- Use repo-root-relative paths.
- Do not hardcode machine-specific absolute paths.
- Read input data from data/raw/.
- Write derived datasets to data/processed/.
- Write figures to output/figures/.
- Write logs to output/logs/.
- Write temporary files to output/tmp/.
- Preserve raw data; never rewrite it in place.

xarray/dask guidance:
- Prefer lazy loading for large datasets.
- Avoid premature .values, .load(), or .compute().
- Rechunk intentionally when inherited chunks are poor.
- Reduce before materializing large arrays.
- Preserve coordinate metadata when practical.

For non-trivial runs:
- Write a short run log with inputs, outputs, parameters, and notable choices.
- Verify expected outputs exist before reporting completion.
"""
